% Set the style and provide helper functions for binary trees
% by Sean T. Hayes
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{binary-trees}[2025/08/21]

\RequirePackage{forest}
\usetikzlibrary{positioning}



\definecolor{highlightColor}{HTML}{ffd7aa}

\tikzset{
  %
  % Used for beamer overlays
  invisible/.style={opacity=0,text opacity=0},
  visible on/.style={alt=#1{}{invisible}},
  invisible on/.style={only=#1{invisible}},
  alt/.code args={<#1>#2#3}{\alt<#1>{\pgfkeysalso{#2}}{\pgfkeysalso{#3}}},
  only/.code args={<#1>#2}{\only<#1>{\pgfkeysalso{#2}}},
  justText/.style={ellipse,draw=none, inner sep=1pt, fill=none},
  justText on/.style={only=#1{justText}},
  %
  % The following is used to highlight edges as an overlay
  highlight/.style={draw, ultra thick, color=highlightColor},
  highlight on/.style={only=#1{highlight}},
}

\forestset{
  treeNode/.style={%
    circle, % node shape
    draw, %line and border color
    edge=->, % show arrow to child
    edge=very thick, %edge weight
    very thick, % node boarder
    fill=CSUcyan, % node fill
    text=black,% text color
    %fit=rectangle, % the resulting tree will usually be wide
    fit=band,%
    inner sep=3pt, % space between text and border
    l sep=0em,% distance between levels
  },
  default preamble={%
    for tree={%
      treeNode%
    },%
  },%
  %
  headPtr/.style={
    anchor=north,l sep=0.5em,%
    inner sep=0,
    outer sep=0,
    minimum size=0,
    ellipse,
    draw=none,
    fill=none,
    text=fg
  },
  %
  % Used for beamer overlays
  visible on/.style={%
    for tree={%
      /tikz/visible on={#1},%
      edge+={/tikz/visible on={#1}}}},%
  %
  % Hide just the node, not it's children (and the edges connecting it)
  node visible on/.style={%
      /tikz/visible on={#1},%
      edge+={/tikz/visible on={#1}},%
      for children={edge+={/tikz/visible on={#1}}}%
  },%
  %
  edge highlight on/.style={
    edge+={/tikz/highlight on={#1}}
  },%
  %
  justText on/.style={%
    for tree={%
      /tikz/justText on={#1},%
      % edge={/tikz/justText on={#1}}
    }%
  },%
  %
  textColorDraw/.style={
    draw, % enables border drawing
    every node/.append style={color=text color},
  },
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% For drawing null pointers
\usetikzlibrary{circuits.ee.IEC}

\tikzset{
  %
  % Used for Beamer overlays
  empty/.style={ground, draw=none},
}

\forestset{
  ground leaf/.style={
    ellipse,
    fill=none,
    draw=none,
    tikz+={%
      \node[ground IEC, rotate=90, draw, thick, xscale=0.7, yscale=1.5]
        at (.south) {};
    },
    minimum height=0.3em,
    minimum width=1.6em,
  },
}

\newsavebox\nullNode
\savebox\nullNode{%
  \begin{tikzpicture}[circuit ee IEC,scale=1]
  \node[empty,rotate=90, draw=bg, thick] {};
  \end{tikzpicture}%
}

\makeatletter\def\NullNode{%
  \usebox{\nullNode}
}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%